version: "3.8"

services:
  api:
    image: node:18
    working_dir: /app
    volumes:
      - ./server:/app
    command: sh -c "npm install && npm run compile && npm start"
    ports:
      - 8080:8080
    environment:
      - NODE_ENV=production
